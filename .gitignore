/target
TODO-LIST.md

# Tool-specific agent pointers (thin @AGENTS.md imports, never committed)
CLAUDE.md
GEMINI.md
.cursor/
.aider*
.windsurfrules

# Private reference material, never committed - see AGENTS.md
/.nocommit

# The demo rig's scratch: fixtures, the staged home, the shell it opens.
# demo/*.tape and demo/stage.sh are the source of every README asset and are
# committed; nothing under here is.
/demo/home
/demo/bin
/demo/shellrc
