/target
TODO-LIST.md

# Tool-specific agent pointers (thin @AGENTS.md imports, never committed)
CLAUDE.md
GEMINI.md
.cursor/
.aider*
.windsurfrules

# Private reference material, never committed - see AGENTS.md
/.nocommit

# The demo rig's scratch: the staged home it builds, which holds the fixtures,
# the `vibox` symlink and the rc the staged shell reads. demo/*.tape and
# demo/stage.sh are the source of every README asset and are committed.
/demo/home
